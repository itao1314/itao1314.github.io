<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Swift-集合类型</title>
      <link href="/2019/05/28/Swift-%E9%9B%86%E5%90%88%E7%B1%BB%E5%9E%8B/"/>
      <url>/2019/05/28/Swift-%E9%9B%86%E5%90%88%E7%B1%BB%E5%9E%8B/</url>
      
        <content type="html"><![CDATA[<ol><li><p><code>Swift</code>用<code>Array</code>、<code>Sets</code>、<code>Dictionaries</code>三种基本的集合类型用来存储集合数据。</p></li><li><p><code>Array</code>是有序数据的集。</p></li><li><p><code>Sets</code>是无序无重复数据的集。</p></li><li><p><code>Dictionaries</code>是无序的键值对的集。</p><p> <img src="/2019/05/28/Swift-集合类型/CollectionTypes_intro_2x.png" alt="BP"></p></li><li><p><code>Swift</code>的集合类型存储的数据值类型必须明确。</p></li><li><p>创建一个数组：</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> someArray = [<span class="type">Element</span>]() <span class="comment">//空数组，Element是这个数组中唯一允许存在的数据类型。</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> shoppingList: [<span class="type">String</span>] = [<span class="string">"Eggs"</span>, <span class="string">"Milk"</span>]<span class="comment">//数组字面量构造数组</span></span><br></pre></td></tr></table></figure></li><li><p>使用下标来一次改变一系列的数据值，即使新数据和原有数据的数量不一样。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">shoppingList[<span class="number">4</span>...<span class="number">6</span>] = [<span class="string">"Bananas"</span>, <span class="string">"Apples"</span>]</span><br><span class="line"><span class="comment">// shoppingList 现在有6项</span></span><br></pre></td></tr></table></figure></li><li><p>不可以使用下标访问的形式去在数组尾部添加一个新元素。</p></li><li><p>一个类型必须是可哈希化的才可以存储在<code>Sets</code>中。</p></li><li><p>哈希值是<code>Int</code>类型的，相等的对象哈希值必须是相同的。</p></li><li><p><code>Swift</code>的基本数据类型都是可哈希化得。</p></li><li><p>没有关联值的枚举成员值默认也是可哈希化的。</p></li><li><p>自定义类型的想要做为<code>Sets</code>的值或者<code>Dictionaries</code>的<code>key</code>，必须实现<code>Hashable</code>协议。</p></li><li><p>创建一个空<code>Sets</code>：</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> someElement = <span class="type">Set</span>&lt;<span class="type">Element</span>&gt;()<span class="comment">//Sets没有简化的等价形式</span></span><br></pre></td></tr></table></figure></li><li><p>使用数组字面量创建集合</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> favoriteGenres: <span class="type">Set</span>&lt;<span class="type">String</span>&gt; = [<span class="string">"Rock"</span>, <span class="string">"Classical"</span>, <span class="string">"Hip hop"</span>]</span><br><span class="line"><span class="comment">// favoriteGenres 被构造成含有三个初始值的集合</span></span><br><span class="line"><span class="comment">//一个Set类型不能从数组的字面量中被单独推断出来，因此Set类型必须被显示声明，但是&lt;String&gt;可以被推断。</span></span><br></pre></td></tr></table></figure></li><li><p>集合的操作</p><p><img src="/2019/05/28/Swift-集合类型/setVennDiagram_2x.png" alt="BP"></p></li><li><p><code>Swift</code>中使用<code>Dictionary&lt;Key, Value&gt;</code>定义，简化形式<code>[Key: Value]</code>。</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> namesOfIntegers = [<span class="type">Int</span>: <span class="type">String</span>]()</span><br><span class="line"><span class="comment">// namesOfIntegers 是一个空的 [Int: String] 字典</span></span><br></pre></td></tr></table></figure></li></ol>]]></content>
      
      
      <categories>
          
          <category> iOS开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Swift基础 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Swift-字符串和字符</title>
      <link href="/2019/05/28/Swift-%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%92%8C%E5%AD%97%E7%AC%A6/"/>
      <url>/2019/05/28/Swift-%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%92%8C%E5%AD%97%E7%AC%A6/</url>
      
        <content type="html"><![CDATA[<blockquote><p><code>Swift</code>的字符串通过<code>String</code>类型来表示。<code>Swift</code>中的<code>String</code>类型与<code>Foundation</code>中的<code>NSString</code>类进行了无缝桥接。</p></blockquote><ol><li><p>字符串字面量 ——&gt; <code>&quot;Some string literal value&quot;</code></p></li><li><p>多行字符串字面量-由一对三个双引号包裹着的文本：</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> multilineString = <span class="string">"""</span></span><br><span class="line"><span class="string">These are the same.</span></span><br><span class="line"><span class="string">"""</span></span><br></pre></td></tr></table></figure></li><li><p>字符串字面量中的特殊字符<code>\0(空字符)</code>、<code>\\(反斜线)</code>、<code>\t(水平制表符)</code>、<code>\n(换行符)</code>、<code>\r(回车符)</code>、<code>\&quot;(双引号)</code>、<code>\&#39;(单引号)</code>。</p></li><li><p><code>Swift</code>中<code>String</code>类型是值类型。字符串在传递时进行值拷贝。</p></li><li><p>使用<code>for-in</code>循环来遍历字符串，获取每个字符：</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> character <span class="keyword">in</span> <span class="string">"Dog!🐶"</span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(character)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>字符串可以通过传递一个值类型为<code>Character</code>的数组作为自变量来初始化：</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> catCharacters: [<span class="type">Character</span>] = [<span class="string">"C"</span>, <span class="string">"a"</span>, <span class="string">"t"</span>, <span class="string">"!"</span>, <span class="string">"🐱"</span>]</span><br><span class="line"><span class="keyword">let</span> catString = <span class="type">String</span>(catCharacters)</span><br><span class="line"><span class="built_in">print</span>(catString)</span><br><span class="line"><span class="comment">// 打印输出：“Cat!🐱”</span></span><br></pre></td></tr></table></figure></li><li><p>不能将字符串或者字符添加到一个已经存在的字符变量上，因为字符变量只能包含一个字符。</p></li><li><p>字符串插值<code>\(multiplier)</code>。</p></li><li><p>每一个<code>String</code>值都有一个关联的索引类型，<code>String.index</code>对应字符串中的每一个<code>Character</code>的位置。</p></li><li><p>不同的字符可能会占据不同的内存空间，所以如果要知道一个<code>Character</code>的确定位置，必须从<code>String</code>的开头遍历。因此<code>Swift</code>的字符串不能用整数做索引。</p></li><li><p>使用<code>String</code>的<code>index(before:)</code>、<code>index(after:)</code>、<code>index(_:offsetBy:)</code>方法来获取一个索引。</p></li><li><p><code>Swift</code>中的<code>SubString</code>绝大部分函数与<code>String</code>一样。</p></li><li><p><code>String</code>和<code>SubString</code>的区别在性能上，<code>SubString</code>可以重用原<code>String</code>的内存空间或者另一个<code>SubString</code>的内存空间。</p></li><li><p><code>SubString</code>不合适长期存储–因为重用了原<code>String</code>的内存空间，原<code>String</code>内存空间必须保留知道<code>SubString</code>不再被使用为止。</p></li><li><p>下图展示<code>String</code>和<code>SubString</code>的关系：</p><p><img src="/2019/05/28/Swift-字符串和字符/stringSubstring_2x.png" alt="BP"></p></li><li><p><code>Swift</code>三种方式比较文本值：字符串字符相等、前缀相等。</p></li></ol>]]></content>
      
      
      <categories>
          
          <category> iOS开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Swift基础 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Swift-基本运算符</title>
      <link href="/2019/05/27/Swift-%E5%9F%BA%E6%9C%AC%E8%BF%90%E7%AE%97%E7%AC%A6/"/>
      <url>/2019/05/27/Swift-%E5%9F%BA%E6%9C%AC%E8%BF%90%E7%AE%97%E7%AC%A6/</url>
      
        <content type="html"><![CDATA[<h2 id="赋值运算符"><a href="#赋值运算符" class="headerlink" title="赋值运算符"></a>赋值运算符</h2><ol><li><p>赋值的右边是一个多元组，它的元素会被分解多个常量和变量。</p><pre><code><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> (x, y) = (<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line"><span class="comment">// 现在 x 等于 1，y 等于 2</span></span><br></pre></td></tr></table></figure></code></pre></li><li><p>Swift的赋值操作并不返回任何值。<code>C</code>和<code>OC</code>是返回值得。</p></li></ol><h2 id="算术运算符"><a href="#算术运算符" class="headerlink" title="算术运算符"></a>算术运算符</h2><ol><li><code>Swift</code>默认情况下不允许在数值运算中出现溢出的情况。</li></ol><h2 id="组合赋值运算符"><a href="#组合赋值运算符" class="headerlink" title="组合赋值运算符"></a>组合赋值运算符</h2><ol><li><code>let b = a += 2</code>是错误的，因为复合赋值运算没有返回值，<code>a += 2</code>是没有返回值的。</li></ol><h2 id="比较运算符"><a href="#比较运算符" class="headerlink" title="比较运算符"></a>比较运算符</h2><ol><li><p><code>Swift</code> 也提供恒等<code>（===）</code>和不恒等<code>（!==）</code>这两个比较符来判断两个对象是否引用同一个对象实例。</p></li><li><p>元组的元素相同，且长度相同，元组就可以被比较。元组的比较会从左到右逐个比较，知道发现有两个值相等时停止。</p><pre><code><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(<span class="number">1</span>, <span class="string">"zebra"</span>) &lt; (<span class="number">2</span>, <span class="string">"apple"</span>)   <span class="comment">// true，因为 1 小于 2</span></span><br><span class="line">(<span class="number">3</span>, <span class="string">"apple"</span>) &lt; (<span class="number">3</span>, <span class="string">"bird"</span>)    <span class="comment">// true，因为 3 等于 3，但是 apple 小于 bird</span></span><br><span class="line">(<span class="number">4</span>, <span class="string">"dog"</span>) == (<span class="number">4</span>, <span class="string">"dog"</span>)      <span class="comment">// true，因为 4 等于 4，dog 等于 dog</span></span><br></pre></td></tr></table></figure></code></pre></li><li><p><code>Bool</code>不能被比较，所以含有布尔类型的元组不能被比较。</p></li><li><p><code>Swift</code> 标准库只能比较七个以内元素的元组比较函数。如果你的元组元素超过七个时，你需要自己实现比较运算符。</p></li></ol><h2 id="空合运算符"><a href="#空合运算符" class="headerlink" title="空合运算符"></a>空合运算符</h2><ol><li>空合运算符<code>(a ?? b)</code>将对可选类型<code>a</code>进行空判断，如果<code>a</code>包含一个值就解包，否则返回一个默认值<code>b</code>。表达式 <code>a</code> 必须是 <code>Optional</code> 类型。默认值 <code>b</code> 的类型必须要和 <code>a</code> 存储值的类型保持一致。</li><li><p>空合运算符是对下面代码的简写：</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">a != <span class="literal">nil</span> ? a! : b</span><br></pre></td></tr></table></figure></li></ol><h2 id="区间运算符"><a href="#区间运算符" class="headerlink" title="区间运算符"></a>区间运算符</h2><ol><li><p>闭区间运算符<code>(a...b)</code>定义一个从<code>a</code>到<code>b</code>的所有值的区间。</p></li><li><p>半开区间运算符<code>(a..&lt;b)</code>定义一个从<code>a</code>到<code>b</code>但不包含<code>b</code>的区间。</p></li></ol><h3 id="单侧区间"><a href="#单侧区间" class="headerlink" title="单侧区间"></a>单侧区间</h3><ol><li><p>闭区间操作符有另一表达形式，表达往一侧无限延伸的区间。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> name <span class="keyword">in</span> names[<span class="number">2</span>...] &#123;</span><br><span class="line">    <span class="built_in">print</span>(name)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Brian</span></span><br><span class="line"><span class="comment">// Jack</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> name <span class="keyword">in</span> names[...<span class="number">2</span>] &#123;</span><br><span class="line">    <span class="built_in">print</span>(name)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Anna</span></span><br><span class="line"><span class="comment">// Alex</span></span><br><span class="line"><span class="comment">// Brian</span></span><br></pre></td></tr></table></figure></li><li><p>半开区间操作符也有单侧表达式。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> name <span class="keyword">in</span> names[..&lt;<span class="number">2</span>] &#123;</span><br><span class="line">    <span class="built_in">print</span>(name)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Anna</span></span><br><span class="line"><span class="comment">// Alex</span></span><br></pre></td></tr></table></figure></li><li><p>不能遍历省略了初始值的单侧区间，因为遍历的开端并不明显。</p></li></ol>]]></content>
      
      
      <categories>
          
          <category> iOS开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Swift基础 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Swift-基础部分</title>
      <link href="/2019/05/24/Swift-%E5%9F%BA%E7%A1%80%E9%83%A8%E5%88%86/"/>
      <url>/2019/05/24/Swift-%E5%9F%BA%E7%A1%80%E9%83%A8%E5%88%86/</url>
      
        <content type="html"><![CDATA[<ol><li><code>Siwft</code>包含<code>C</code>和<code>OC</code>上所有的基本数据类型。还有三个基本的几个类型，<code>Array</code>、<code>Set</code>、<code>Dictionary</code>。</li><li>常量：值不可改变的变量。</li><li><p>元祖（Tuple）可以创建或着传递一组数据，是<code>Swift</code>的高阶数据类型。元祖内的值可以是任意类型的，并不要求是相同类型。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> http404Error = (<span class="number">404</span>, <span class="string">"Not Found"</span>)</span><br><span class="line"><span class="comment">// http404Error 的类型是 (Int, String)，值是 (404, "Not Found")</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> (statusCode, statusMessage) = http404Error <span class="comment">//元组的内容分解</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> (justTheStatusCode, <span class="number">_</span>) = http404Error <span class="comment">//_忽略这个值</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">"The status code is \(http404Error.0)"</span>)<span class="comment">//可以通过下标来访问元组的值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> http200Status = (statusCode: <span class="number">200</span>, description: <span class="string">"OK"</span>)<span class="comment">//定义元组时可以给元素命名，然后就可以通过名称来获取元素的值</span></span><br></pre></td></tr></table></figure></li><li><p><code>Swift</code>用可选类型（Optional）来处理值缺失的情况，可用在任何类型上。</p></li><li><code>Swift</code>是一门类型安全的语言。在编译时进行代码的类型检查，把不匹配的类型标记为错误。</li><li>常量的值一旦设定就不能改变，变量的值可以随意更改。</li><li>用<code>let</code>声明常量，<code>var</code>声明变量。</li><li><p>声明常量或者变量的时候可以加上类型标注，说明常量或者变量中要存储的值的类型。例子:</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> welcomeMessage: <span class="type">String</span></span><br></pre></td></tr></table></figure></li><li><p><code>Swift</code>不用语句结尾处添加分号<code>;</code>,在同一行内写多条独立的语句时必须使用分号分隔。</p></li><li><p>可以使用不同整数类型的<code>min</code>和<code>max</code>属性来获取最小值和最大值。</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> minValue = <span class="type">UInt8</span>.<span class="built_in">min</span>  <span class="comment">// minValue 为 0，是 UInt8 类型</span></span><br><span class="line"><span class="keyword">let</span> maxValue = <span class="type">UInt8</span>.<span class="built_in">max</span>  <span class="comment">// maxValue 为 255，是 UInt8 类型</span></span><br></pre></td></tr></table></figure></li><li><p><code>Int</code>类型在32位平台上和<code>Int32</code>长度相同，在64位平台上和<code>Int64</code>长度相同。</p></li><li><code>Double</code>表示64位浮点数，需要高精度浮点数时使用。<code>Float</code>表示32位浮点数。</li><li>通过给变量和常量在声明时赋初始值，让编译器在编译代码的时候可以自动推断出变量和常量的类型，这就是<code>Swift</code>的类型推断。</li><li>推断浮点数的类型时，<code>Swift</code>总是会选择<code>Double</code>。</li><li><p><code>SomeType(ofInitialValue)</code>是调用<code>Swift</code>构造器并传入一个初始值的默认方法。可以用来类型转换。</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> twoThousand: <span class="type">UInt16</span> = <span class="number">2_000</span></span><br><span class="line"><span class="keyword">let</span> one: <span class="type">UInt8</span> = <span class="number">1</span></span><br><span class="line"><span class="keyword">let</span> twoThousandAndOne = twoThousand + <span class="type">UInt16</span>(one)</span><br></pre></td></tr></table></figure></li><li><p>整数和浮点数之间的转换必须显示指定类型。</p></li><li>结合数字类常量和变量不同于结合数字类字面量。数字字面量本身没有类型，它的类型只在编译器需要求值的时候被推测。</li><li><p>类型别名就是给现有类型定义另一个名字。使用关键字<code>typealias</code>来定义。</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typealias</span> <span class="type">AudioSample</span> = <span class="type">UInt16</span> <span class="comment">//AudioSample 被定义为 UInt16 的一个别名</span></span><br></pre></td></tr></table></figure></li><li><p><code>Bool</code>是<code>Swift</code>的布尔类型，值是<code>true</code>和 <code>false</code>。</p></li></ol><h2 id="可选类型"><a href="#可选类型" class="headerlink" title="可选类型"></a>可选类型</h2><ol><li>使用可选类型来处理值可能缺失的情况。</li><li><code>Int?</code>表示一个可选<code>Int</code>，表示可能包含值，也可能不包含值。</li><li>可选变量可以被赋值为<code>nil</code>来表示没有值。</li><li><code>nil</code>不能用于非可选的变量和常量。</li><li>声明的可选常量和变量如果没有初始值，自动被置为<code>nil</code>。</li><li><code>Swift</code>和<code>OC</code>中的<code>nil</code>是不一样的。<code>OC</code>中<code>nil</code>指向一个不存在对象的指针。·<code>Swift</code>中<code>nil</code>是一个确定的值，用来表示值缺失，任何类型的可选状态都可以被置为<code>nil</code>。</li><li>当确定一个可选类型确实包含值，可以使用<code>!</code>来获取值，这被称为可选值的强制解析。</li><li><p>使用可选绑定来判断可选类型是否包含值，如果包含就把值赋给一个临时的变量或者常量。在<code>if</code>和<code>while</code>语句中使用可选绑定，可以判断可选类型中是否有值。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="keyword">let</span> constantName = someOptional &#123;</span><br><span class="line">      statements</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></li><li><p>在<code>if</code>语句中使用可选绑定将值赋给常量或者变量，仅在<code>if</code>语句的句中才能获取到值。</p></li></ol><h2 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h2><ol><li><p>使用错误处理来应对程序中可能遇到的错误，错误处理可以推断失败的原因，并传播至程序的其他部分。</p></li><li><p>一个函数可以在通过声明中添加<code>throws</code>关键字来抛出错误消息，调用函数时应该使用<code>try</code>关键字。一个<code>do</code>语句创建了一个新的包含作用域，错误可以被传播到一个或多个<code>catch</code>从句。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> canThrowAnError()</span><br><span class="line">    <span class="comment">// 没有错误消息抛出</span></span><br><span class="line">&#125; <span class="keyword">catch</span> &#123;</span><br><span class="line">    <span class="comment">// 有一个错误消息抛出</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h2 id="断言和先决条件"><a href="#断言和先决条件" class="headerlink" title="断言和先决条件"></a>断言和先决条件</h2><ol><li><p>断言和先决条件是在运行时所作的检查，用来检查在执行后续代码之前是否一个必要的条件已经被满足。</p></li><li><p>断言和先决条件的不同点：断言仅在调试环境运行，而先决条件则在调试环境和生产环境中运行。</p></li><li><p>可以在开发阶段使用断言，这些断言在生产环境中不会产生任何影响。</p></li><li><p>使用<code>assert(_:_:file:line:)</code>函数来写一个断言。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> age = -<span class="number">3</span></span><br><span class="line"><span class="built_in">assert</span>(age &gt;= <span class="number">0</span>, <span class="string">"A person's age cannot be less than zero"</span>)</span><br><span class="line"><span class="comment">// 因为 age &lt; 0，所以断言会触发</span></span><br></pre></td></tr></table></figure></li><li><p>使用<code>precondition(_:_:file:line:)</code>函数写一个先决条件。</p> <figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 在一个下标的实现里...</span></span><br><span class="line"><span class="built_in">precondition</span>(index &gt; <span class="number">0</span>, <span class="string">"Index must be greater than zero."</span>)</span><br></pre></td></tr></table></figure></li></ol>]]></content>
      
      
      <categories>
          
          <category> iOS开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Swift基础 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
